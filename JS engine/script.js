// Every JS engine requires a JRE. A run a JS code we need a JS engine, Where JRE has all the requirements to run JS Engine.
// JS engine is like the heart of the JRE, without JS engine no use of JRE.

// Browser has a JRE, NodeJS has a JRE.

// JS Engine

// CODE -> PARSING -> COMPILATION -> EXECUTION